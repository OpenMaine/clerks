<div class="wrapper">
  <%= render "shared/sidebar" %>

  <div class="main">

    <div class="content">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item" aria-current="page">Campaigns</li>
          <li class="breadcrumb-item active" aria-current="page"><%= @campaign.name %></li>
        </ol>
      </nav>

      <div id="<%= dom_id @campaign %>" class="candidate-header">
        <h3 class="candidate-name-header">
          <div><%= @campaign.name %></div>
        </h3>
        <div>
          <%= @campaign.office + " Candidate" %>
        </div>
        <div>
          <%= @campaign.election.title %>
        </div>
      </div>

      <div class="container-fluid">
        <div class="row-fluid">
          <div class="span8">
              <ul class="nav nav-tabs card-header-tabs">
                  <li class="nav-item">
                    <%= link_to "Summary", @campaign, class: "nav-link"%>
                  </li>
                  <li class="nav-item">
                    <%= link_to "Filings", campaign_campaign_reports_path(@campaign.id), class: "nav-link" %>
                  </li>
                  <li class="nav-item active">
                    <%= link_to "Contact", contact_campaign_path, class: "nav-link active-custom" %>
                  </li>
              </ul>
          </div>
        </div>

        <div class="campaign-contact-content">
            <div class="campaign-contact-box">
                <h2>Contact</h2>
                <div class="campaign-contact-container">
                <div>
                    <h3>Address</h3>
                    <%= @campaign.street %>
                    <%= @campaign.city %>
                </div>
                <div>
                    <h3>Phone</h3>
                    <%= @campaign.telephone %>
                </div>
                <div>
                    <h3>Email</h3>
                    <%= @campaign.email %>
                </div>
                </div>
            </div>
            </div>

      <hr>

    </div>

  </div>
</div>