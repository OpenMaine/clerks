json.extract! campaign_schedule_c, :id, :campaign_id, :lender, :balance_at_beginning, :amount_loaned, :amount_repaid, :amount_forgiven, :balance, :created_at, :updated_at
json.url campaign_schedule_c_url(campaign_schedule_c, format: :json)
