<p style="color: green"><%= notice %></p>

<p>
  <strong>Campaign:</strong>
  <%= @campaign_report.campaign.name %>
</p>


<h3>Campaign Schedule A</h3>
<div id="campaign_schedule_as">
  <%= render @campaign_schedule_as %>
</div>
<%= link_to "Back to campaign report", campaign_campaign_report_path(id: @campaign_report) %>
